<%= form_with(model: shoe_model) do |form| %>
  <% if shoe_model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shoe_model.errors.count, "error") %> prohibited this shoe_model from being saved:</h2>

      <ul>
        <% shoe_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
